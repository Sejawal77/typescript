"use strict";
function city_country(city, country) {
    return `${city}, ${country}`;
}
// Call the function
const formattedLocation = city_country("Lahore", "Pakistan");
console.log(formattedLocation); // Output: Lahore, Pakistan
